interface:
  display_name: "咔嚓"
  short_description: "完整首剪与分层增量返工并行、自动 QC 与人工审片共同放行"
  default_prompt: "完整读取咔嚓 SKILL.md，先判断任务应走 v2 完整首剪还是 v3 增量返工，再只读取相关 references 并执行对应门禁。严格保持源素材只读、用户未授权时保持原几何、冻结层用流哈希证明、变化层专项验收，最终发布仍需当前版本完整 QC 和人工审片。"
